public class tt {
    public static void main(String[] args) {
        ArrayList<Double> coef = new ArrayList<Double>();
        coef.add(null);
        System.out.println(Collections.max(coef).doubleValue());
    }
}
